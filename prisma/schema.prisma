// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model AgentAdvanceLedger {
  amount_deducted Float
  closing_balance Float
  created_at String
  date String
  deduction_status String
  id String @id @default(uuid())
  interest_accrued Float
  opening_balance Float

  advance_id String?
  advance_rel AgentAdvances? @relation("agent_advance_ledger_advance_id", fields: [advance_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("agent_advance_ledger")
}

model AgentAdvanceTopups {
  amount Float
  created_at String
  id String @id @default(uuid())
  topped_up_by String

  advance_id String?
  advance_rel AgentAdvances? @relation("agent_advance_topups_advance_id", fields: [advance_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  topped_up_by_rel Profiles? @relation("agent_advance_topups_topped_up_by", fields: [topped_up_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("agent_advance_topups")
}

model AgentAdvances {
  created_at String
  cycle_days Float
  daily_rate Float
  expires_at String
  id String @id @default(uuid())
  issued_at String
  issued_by String
  outstanding_balance Float
  principal Float
  registration_fee Float?
  status String
  updated_at String

  agent_id String?
  agent_rel Profiles? @relation("agent_advances_agent_id", fields: [agent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  issued_by_rel Profiles? @relation("agent_advances_issued_by", fields: [issued_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  agent_advance_topups_advance_id AgentAdvanceTopups[] @relation("agent_advance_topups_advance_id")
  agent_advance_ledger_advance_id AgentAdvanceLedger[] @relation("agent_advance_ledger_advance_id")
  @@map("agent_advances")
}

model AgentCollections {
  amount Float
  created_at String
  float_after Float
  float_before Float
  id String @id @default(uuid())
  payment_method String

  agent_id String?
  agent_rel Profiles? @relation("agent_collections_agent_id", fields: [agent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenant_id String?
  tenant_rel Profiles? @relation("agent_collections_tenant_id", fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  token_id String?
  token_rel PaymentTokens? @relation("agent_collections_token_id", fields: [token_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("agent_collections")
}

model AgentCommissionPayouts {
  amount Float
  created_at String
  id String @id @default(uuid())
  mobile_money_number String
  mobile_money_provider String
  processed_at String?
  processed_by String?
  rejection_reason String?
  requested_at String
  status String
  transaction_id String?
  updated_at String

  @@map("agent_commission_payouts")
}

model AgentEarnings {
  amount Float
  created_at String
  description String?
  earning_type String
  id String @id @default(uuid())
  rent_request_id String?
  source_user_id String?

  @@map("agent_earnings")
}

model AgentFloatLimits {
  assigned_by String?
  collected_today Float
  created_at String
  float_limit Float
  id String @id @default(uuid())
  last_reset_date String
  updated_at String

  agent_id String?
  agent_rel Profiles? @relation("agent_float_limits_agent_id", fields: [agent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  assigned_by_rel Profiles? @relation("agent_float_limits_assigned_by", fields: [assigned_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("agent_float_limits")
}

model AgentGoals {
  created_at String
  goal_month String
  id String @id @default(uuid())
  notes String?
  target_activations Float?
  target_registrations Float
  updated_at String

  @@map("agent_goals")
}

model AgentReceipts {
  amount Float
  created_at String
  id String @id @default(uuid())
  notes String?
  payer_name String
  payer_phone String
  payment_method String
  receipt_image_url String?
  transaction_id String?

  agent_id String?
  agent_rel Profiles? @relation("agent_receipts_agent_id", fields: [agent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("agent_receipts")
}

model AgentSubagents {
  created_at String
  id String @id @default(uuid())
  source String

  @@map("agent_subagents")
}

model AgentVisits {
  accuracy Float?
  checked_in_at String
  created_at String
  id String @id @default(uuid())
  latitude Float
  longitude Float

  agent_id String?
  agent_rel Profiles? @relation("agent_visits_agent_id", fields: [agent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenant_id String?
  tenant_rel Profiles? @relation("agent_visits_tenant_id", fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  payment_tokens_visit_id PaymentTokens[] @relation("payment_tokens_visit_id")
  @@map("agent_visits")
}

model AiChatMessages {
  content String
  created_at String
  id String @id @default(uuid())
  role String

  @@map("ai_chat_messages")
}

model CartItems {
  created_at String
  id String @id @default(uuid())
  quantity Float
  updated_at String

  product_id String?
  product_rel Products? @relation("cart_items_product_id", fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("cart_items")
}

model ConversationParticipants {
  id String @id @default(uuid())
  joined_at String
  last_read_at String?

  conversation_id String?
  conversation_rel Conversations? @relation("conversation_participants_conversation_id", fields: [conversation_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("conversation_participants")
}

model Conversations {
  created_at String
  id String @id @default(uuid())
  updated_at String

  messages_conversation_id Messages[] @relation("messages_conversation_id")
  conversation_participants_conversation_id ConversationParticipants[] @relation("conversation_participants_conversation_id")
  @@map("conversations")
}

model CreditAccessLimits {
  base_limit Float
  bonus_from_landlord_rent Float
  bonus_from_ratings Float
  bonus_from_receipts Float
  bonus_from_rent_history Float
  created_at String
  id String @id @default(uuid())
  total_limit Float?
  updated_at String

  @@map("credit_access_limits")
}

model CreditRequestDetails {
  agent_id String?
  agent_verified Boolean?
  agent_verified_at String?
  borrower_mm_name String
  borrower_phone String
  created_at String
  duration_days Float
  electricity_meter_number String?
  funder_interest_rate Float
  id String @id @default(uuid())
  landlord_id String?
  landlord_name String
  landlord_on_platform Boolean?
  landlord_phone String
  loan_id String?
  location_address String?
  location_latitude Float?
  location_longitude Float?
  platform_fee_amount Float
  platform_fee_rate Float
  repayment_frequency String
  total_with_fees Float
  updated_at String
  water_meter_number String?

  loan_rel UserLoans? @relation("credit_request_details_loan_id", fields: [loan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("credit_request_details")
}

model DepositRequests {
  agent_id String?
  amount Float
  approved_at String?
  created_at String
  id String @id @default(uuid())
  notes String?
  processed_by String?
  provider String?
  rejected_at String?
  rejection_reason String?
  status String
  transaction_date String?
  transaction_id String?
  updated_at String

  @@map("deposit_requests")
}

model EarningBaselines {
  id String @id @default(uuid())
  avg_daily_earnings Float?
  avg_receipts_per_day Float?
  avg_referrals_per_week Float?
  avg_weekly_earnings Float?
  last_calculated_at String?
  receipt_count_7d Float?
  referral_count_7d Float?
  total_agent_earnings Float?

  @@map("earning_baselines")
}

model EarningPredictions {
  assumptions Json?
  confidence Float
  created_at String
  id String @id @default(uuid())
  period String
  predicted_earnings Float

  @@map("earning_predictions")
}

model GeneralLedger {
  account String?
  amount Float
  category String
  created_at String
  description String?
  direction String
  id String @id @default(uuid())
  linked_party String?
  reference_id String?
  running_balance Float?
  source_id String?
  source_table String
  transaction_date String
  transaction_group_id String?
  user_id String?

  @@map("general_ledger")
}

model InvestmentWithdrawalRequests {
  amount Float
  created_at String
  earliest_process_date String
  id String @id @default(uuid())
  processed_at String?
  processed_by String?
  reason String?
  rejection_reason String?
  requested_at String
  rewards_paused Boolean
  status String
  updated_at String

  @@map("investment_withdrawal_requests")
}

model InvestorPortfolios {
  account_name String?
  account_number String?
  activation_token String
  bank_name String?
  created_at String
  duration_months Float
  id String @id @default(uuid())
  investment_amount Float
  investor_id String?
  invite_id String?
  maturity_date String?
  mobile_money_number String?
  mobile_network String?
  next_roi_date String?
  payment_method String?
  portfolio_code String
  portfolio_pin String
  roi_mode String
  roi_percentage Float
  status String
  total_roi_earned Float

  investor_rel Profiles? @relation("investor_portfolios_investor_id", fields: [investor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  invite_rel SupporterInvites? @relation("investor_portfolios_invite_id", fields: [invite_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  agent_id String?
  agent_rel Profiles? @relation("investor_portfolios_agent_id", fields: [agent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("investor_portfolios")
}

model Landlords {
  account_number String?
  bank_name String?
  caretaker_name String?
  caretaker_phone String?
  cell String?
  country String?
  county String?
  created_at String
  description String?
  desired_rent_from_welile Float?
  district String?
  electricity_meter_number String?
  has_smartphone Boolean?
  house_category String?
  house_number String?
  id String @id @default(uuid())
  is_agent_managed Boolean?
  latitude Float?
  location_captured_at String?
  location_captured_by String?
  longitude Float?
  managed_by_agent_id String?
  management_fee_rate Float?
  mobile_money_name String?
  mobile_money_number String?
  monthly_rent Float?
  name String
  number_of_houses Float?
  number_of_rooms Float?
  phone String
  property_address String
  ready_to_receive Boolean?
  region String?
  registered_by String?
  rent_balance_due Float
  rent_last_paid_amount Float?
  rent_last_paid_at String?
  sub_county String?
  tenant_id String?
  tin String?
  town_council String?
  verification_pin_1 String?
  verification_pin_2 String?
  verified Boolean?
  verified_at String?
  verified_by String?
  village String?
  water_meter_number String?

  verified_by_rel Profiles? @relation("landlords_verified_by", fields: [verified_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  rent_requests_landlord_id RentRequests[] @relation("rent_requests_landlord_id")
  @@map("landlords")
}

model Lc1Chairpersons {
  created_at String
  id String @id @default(uuid())
  name String
  phone String
  village String

  rent_requests_lc1_id RentRequests[] @relation("rent_requests_lc1_id")
  @@map("lc1_chairpersons")
}

model LedgerAccountGroups {
  id String @id @default(uuid())
  description String
  group_code String

  ledger_accounts_group_id LedgerAccounts[] @relation("ledger_accounts_group_id")
  @@map("ledger_account_groups")
}

model LedgerAccounts {
  id String @id @default(uuid())
  account_code String
  allow_negative Boolean?
  created_at String?
  currency String?
  owner_id String?
  owner_type String?

  group_id String?
  group_rel LedgerAccountGroups? @relation("ledger_accounts_group_id", fields: [group_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  ledger_entries_account_id LedgerEntries[] @relation("ledger_entries_account_id")
  @@map("ledger_accounts")
}

model LedgerEntries {
  id String @id @default(uuid())
  amount Float
  created_at String
  currency String?
  direction String

  transaction_id String?
  transaction_rel LedgerTransactions? @relation("ledger_entries_transaction_id", fields: [transaction_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  account_id String?
  account_rel LedgerAccounts? @relation("ledger_entries_account_id", fields: [account_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("ledger_entries")
}

model LedgerTransactions {
  id String @id @default(uuid())
  approved_by String?
  category String
  created_at String
  description String?
  initiated_by String?
  source_id String?
  source_table String?
  transaction_group_id String?

  transaction_approvals_transaction_id TransactionApprovals[] @relation("transaction_approvals_transaction_id")
  ledger_entries_transaction_id LedgerEntries[] @relation("ledger_entries_transaction_id")
  @@map("ledger_transactions")
}

model LoanApplications {
  amount Float
  created_at String
  id String @id @default(uuid())
  status String
  updated_at String

  @@map("loan_applications")
}

model LocationRequests {
  accuracy Float?
  captured_at String?
  created_at String
  id String @id @default(uuid())
  latitude Float?
  longitude Float?
  requested_by String
  status String
  target_role String
  token String

  rent_request_id String?
  rent_request_rel RentRequests? @relation("location_requests_rent_request_id", fields: [rent_request_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("location_requests")
}

model Messages {
  content String
  created_at String
  id String @id @default(uuid())
  read_at String?

  conversation_id String?
  conversation_rel Conversations? @relation("messages_conversation_id", fields: [conversation_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("messages")
}

model MoneyRequests {
  amount Float
  created_at String
  description String?
  id String @id @default(uuid())
  responded_at String?
  status String

  @@map("money_requests")
}

model Notifications {
  created_at String?
  id String @id @default(uuid())
  is_read Boolean?
  message String
  metadata Json?
  title String
  type String
  updated_at String?

  @@map("notifications")
}

model OnboardingTargets {
  achieved_count Float
  created_at String
  id String @id @default(uuid())
  target_count Float
  target_month String
  updated_at String

  @@map("onboarding_targets")
}

model OpportunitySummaries {
  created_at String
  id String @id @default(uuid())
  notes String?
  posted_by String
  total_agents Float
  total_landlords Float
  total_rent_requested Float
  total_requests Float
  updated_at String

  @@map("opportunity_summaries")
}

model OtpVerifications {
  attempts Float
  created_at String
  expires_at String
  id String @id @default(uuid())
  otp_code String
  phone String
  updated_at String
  verified Boolean
  verified_at String?

  @@map("otp_verifications")
}

model PaymentTokens {
  amount Float
  created_at String
  expires_at String
  id String @id @default(uuid())
  token_code String
  used Boolean
  used_at String?
  visit_id String?

  agent_id String?
  agent_rel Profiles? @relation("payment_tokens_agent_id", fields: [agent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenant_id String?
  tenant_rel Profiles? @relation("payment_tokens_tenant_id", fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  visit_rel AgentVisits? @relation("payment_tokens_visit_id", fields: [visit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  agent_collections_token_id AgentCollections[] @relation("agent_collections_token_id")
  @@map("payment_tokens")
}

model PendingWalletOperations {
  account String?
  amount Float
  category String
  created_at String
  description String?
  direction String
  id String @id @default(uuid())
  linked_party String?
  metadata Json?
  reference_id String?
  rejection_reason String?
  reviewed_at String?
  reviewed_by String?
  source_id String?
  source_table String
  status String
  transaction_group_id String?
  updated_at String

  @@map("pending_wallet_operations")
}

model ProductCategories {
  color String?
  created_at String
  id String @id @default(uuid())
  name String

  @@map("product_categories")
}

model ProductImages {
  created_at String
  display_order Float
  id String @id @default(uuid())
  image_url String

  product_id String?
  product_rel Products? @relation("product_images_product_id", fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("product_images")
}

model ProductOrders {
  agent_commission Float
  created_at String
  delivery_notes String?
  estimated_delivery_date String?
  id String @id @default(uuid())
  quantity Float
  status String
  status_updated_at String?
  total_price Float
  unit_price Float

  product_id String?
  product_rel Products? @relation("product_orders_product_id", fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  product_reviews_order_id ProductReviews[] @relation("product_reviews_order_id")
  @@map("product_orders")
}

model ProductReviews {
  comment String?
  created_at String
  id String @id @default(uuid())
  order_id String?
  rating Float
  updated_at String

  product_id String?
  product_rel Products? @relation("product_reviews_product_id", fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  order_rel ProductOrders? @relation("product_reviews_order_id", fields: [order_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  review_responses_review_id ReviewResponses[] @relation("review_responses_review_id")
  review_votes_review_id ReviewVotes[] @relation("review_votes_review_id")
  review_images_review_id ReviewImages[] @relation("review_images_review_id")
  @@map("product_reviews")
}

model Products {
  active Boolean
  category String
  created_at String
  description String?
  discount_ends_at String?
  discount_percentage Float?
  id String @id @default(uuid())
  image_url String?
  name String
  price Float
  stock Float
  updated_at String

  product_images_product_id ProductImages[] @relation("product_images_product_id")
  cart_items_product_id CartItems[] @relation("cart_items_product_id")
  wishlists_product_id Wishlists[] @relation("wishlists_product_id")
  product_reviews_product_id ProductReviews[] @relation("product_reviews_product_id")
  product_orders_product_id ProductOrders[] @relation("product_orders_product_id")
  @@map("products")
}

model Profiles {
  agent_type String?
  avatar_url String?
  city String?
  country String?
  country_code String?
  created_at String
  email String
  frozen_at String?
  frozen_reason String?
  full_name String
  id String @id @default(uuid())
  is_frozen Boolean
  last_active_at String?
  mobile_money_number String?
  mobile_money_provider String?
  monthly_rent Float?
  national_id String?
  phone String
  referrer_id String?
  rent_discount_active Boolean
  territory String?
  updated_at String
  verified Boolean
  whatsapp_verified Boolean?
  whatsapp_verified_at String?

  investor_portfolios_agent_id InvestorPortfolios[] @relation("investor_portfolios_agent_id")
  investor_portfolios_investor_id InvestorPortfolios[] @relation("investor_portfolios_investor_id")
  agent_receipts_agent_id AgentReceipts[] @relation("agent_receipts_agent_id")
  agent_collections_tenant_id AgentCollections[] @relation("agent_collections_tenant_id")
  agent_collections_agent_id AgentCollections[] @relation("agent_collections_agent_id")
  agent_advance_topups_topped_up_by AgentAdvanceTopups[] @relation("agent_advance_topups_topped_up_by")
  agent_advances_issued_by AgentAdvances[] @relation("agent_advances_issued_by")
  agent_advances_agent_id AgentAdvances[] @relation("agent_advances_agent_id")
  landlords_verified_by Landlords[] @relation("landlords_verified_by")
  payment_tokens_tenant_id PaymentTokens[] @relation("payment_tokens_tenant_id")
  payment_tokens_agent_id PaymentTokens[] @relation("payment_tokens_agent_id")
  agent_visits_tenant_id AgentVisits[] @relation("agent_visits_tenant_id")
  agent_visits_agent_id AgentVisits[] @relation("agent_visits_agent_id")
  agent_float_limits_assigned_by AgentFloatLimits[] @relation("agent_float_limits_assigned_by")
  agent_float_limits_agent_id AgentFloatLimits[] @relation("agent_float_limits_agent_id")
  rent_requests_manager_verified_by RentRequests[] @relation("rent_requests_manager_verified_by")
  rent_requests_agent_verified_by RentRequests[] @relation("rent_requests_agent_verified_by")
  password_hash String?
  @@map("profiles")
}

model PushSubscriptions {
  auth String
  created_at String
  endpoint String
  id String @id @default(uuid())
  p256dh String
  updated_at String

  @@map("push_subscriptions")
}

model ReceiptNumbers {
  created_at String
  created_by String
  id String @id @default(uuid())
  receipt_code String
  status String
  vendor_amount Float?
  vendor_marked_at String?

  vendor_id String?
  vendor_rel Vendors? @relation("receipt_numbers_vendor_id", fields: [vendor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  user_receipts_receipt_number_id UserReceipts[] @relation("user_receipts_receipt_number_id")
  @@map("receipt_numbers")
}

model ReferralRewards {
  created_at String
  credited Boolean
  credited_at String?
  id String @id @default(uuid())
  rank Float
  referral_count Float
  reward_amount Float
  reward_month String

  @@map("referral_rewards")
}

model Referrals {
  bonus_amount Float
  created_at String
  credited Boolean
  credited_at String?
  first_transaction_bonus_amount Float?
  first_transaction_bonus_credited Boolean?
  first_transaction_bonus_credited_at String?
  id String @id @default(uuid())

  @@map("referrals")
}

model RentRequests {
  access_fee Float
  agent_id String?
  agent_verified Boolean?
  agent_verified_at String?
  agent_verified_by String?
  amount_repaid Float
  approval_comment String?
  approved_at String?
  approved_by String?
  created_at String
  daily_repayment Float
  disbursed_at String?
  duration_days Float
  fund_recipient_id String?
  fund_recipient_name String?
  fund_recipient_type String?
  fund_routed_at String?
  funded_at String?
  house_category String?
  id String @id @default(uuid())
  lc1_id String
  manager_verified Boolean?
  manager_verified_at String?
  manager_verified_by String?
  next_roi_due_date String?
  number_of_payments Float?
  rejected_reason String?
  rent_amount Float
  request_city String?
  request_country String?
  request_fee Float
  request_latitude Float?
  request_longitude Float?
  roi_payments_count Float?
  schedule_status String?
  status String?
  supporter_id String?
  tenant_electricity_meter String?
  tenant_no_smartphone Boolean
  tenant_water_meter String?
  total_repayment Float
  total_roi_paid Float?
  updated_at String

  landlord_id String?
  landlord_rel Landlords? @relation("rent_requests_landlord_id", fields: [landlord_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  lc1_rel Lc1Chairpersons? @relation("rent_requests_lc1_id", fields: [lc1_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  agent_verified_by_rel Profiles? @relation("rent_requests_agent_verified_by", fields: [agent_verified_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  manager_verified_by_rel Profiles? @relation("rent_requests_manager_verified_by", fields: [manager_verified_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  location_requests_rent_request_id LocationRequests[] @relation("location_requests_rent_request_id")
  subscription_charges_rent_request_id SubscriptionCharges[] @relation("subscription_charges_rent_request_id")
  supporter_roi_payments_rent_request_id SupporterRoiPayments[] @relation("supporter_roi_payments_rent_request_id")
  repayments_rent_request_id Repayments[] @relation("repayments_rent_request_id")
  @@map("rent_requests")
}

model Repayments {
  amount Float
  created_at String
  id String @id @default(uuid())

  rent_request_id String?
  rent_request_rel RentRequests? @relation("repayments_rent_request_id", fields: [rent_request_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("repayments")
}

model ReviewImages {
  created_at String
  display_order Float
  id String @id @default(uuid())
  image_url String

  review_id String?
  review_rel ProductReviews? @relation("review_images_review_id", fields: [review_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("review_images")
}

model ReviewResponses {
  created_at String
  id String @id @default(uuid())
  response_text String
  updated_at String

  review_id String?
  review_rel ProductReviews? @relation("review_responses_review_id", fields: [review_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("review_responses")
}

model ReviewVotes {
  created_at String
  id String @id @default(uuid())
  vote_type String

  review_id String?
  review_rel ProductReviews? @relation("review_votes_review_id", fields: [review_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("review_votes")
}

model SubscriptionChargeLogs {
  amount_deducted Float
  charge_amount Float
  charge_date String
  created_at String
  debt_added Float
  id String @id @default(uuid())
  status String
  wallet_balance_after Float?
  wallet_balance_before Float?

  subscription_id String?
  subscription_rel SubscriptionCharges? @relation("subscription_charge_logs_subscription_id", fields: [subscription_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("subscription_charge_logs")
}

model SubscriptionCharges {
  accumulated_debt Float
  agent_charge_count Float
  agent_charged_amount Float
  agent_id String?
  charge_agent_wallet Boolean
  charge_amount Float
  charges_completed Float
  charges_remaining Float
  created_at String
  end_date String?
  frequency String
  id String @id @default(uuid())
  next_charge_date String
  rent_request_id String?
  service_type String
  start_date String
  status String
  tenant_failed_at String?
  total_charged Float
  total_charges_due Float
  updated_at String

  rent_request_rel RentRequests? @relation("subscription_charges_rent_request_id", fields: [rent_request_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  subscription_charge_logs_subscription_id SubscriptionChargeLogs[] @relation("subscription_charge_logs_subscription_id")
  @@map("subscription_charges")
}

model SupporterAgreementAcceptance {
  id String @id @default(uuid())
  accepted_at String
  agreement_version String
  created_at String
  device_info String?
  ip_address String?
  status String

  @@map("supporter_agreement_acceptance")
}

model SupporterInvites {
  account_name String?
  account_number String?
  activated_at String?
  activated_user_id String?
  activation_token String
  bank_name String?
  country String?
  created_at String
  created_by String
  district_city String?
  email String
  full_name String
  id String @id @default(uuid())
  latitude Float?
  location_accuracy Float?
  longitude Float?
  mobile_money_number String?
  mobile_network String?
  national_id String?
  next_of_kin_name String?
  next_of_kin_phone String?
  next_of_kin_relationship String?
  parent_agent_id String?
  payment_method String?
  phone String
  property_address String?
  role String
  status String
  temp_password String?

  investor_portfolios_invite_id InvestorPortfolios[] @relation("investor_portfolios_invite_id")
  @@map("supporter_invites")
}

model SupporterReferrals {
  id String @id @default(uuid())
  bonus_amount Float?
  bonus_credited Boolean?
  bonus_credited_at String?
  created_at String
  first_investment_at String?

  @@map("supporter_referrals")
}

model SupporterRoiPayments {
  created_at String
  due_date String
  id String @id @default(uuid())
  paid_at String?
  payment_number Float
  rent_amount Float
  roi_amount Float
  status String

  rent_request_id String?
  rent_request_rel RentRequests? @relation("supporter_roi_payments_rent_request_id", fields: [rent_request_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("supporter_roi_payments")
}

model SystemEvents {
  id String @id @default(uuid())
  created_at String?
  event_type String
  metadata Json?
  processed Boolean?
  processed_at String?
  related_entity_id String?
  related_entity_type String?
  user_id String?

  @@map("system_events")
}

model TenantAgreementAcceptance {
  id String @id @default(uuid())
  accepted_at String
  agreement_version String
  created_at String
  device_info String?
  ip_address String?
  status String

  @@map("tenant_agreement_acceptance")
}

model TenantMerchantPayments {
  amount Float
  created_at String
  id String @id @default(uuid())
  merchant_name String
  notes String?
  payment_date String
  tenant_id String?
  tenant_phone String?
  updated_at String

  @@map("tenant_merchant_payments")
}

model TenantRatings {
  created_at String
  id String @id @default(uuid())
  rating Float
  updated_at String

  @@map("tenant_ratings")
}

model TransactionApprovals {
  id String @id @default(uuid())
  approval_notes String?
  approved_at String
  approved_by String
  transaction_id String?

  transaction_rel LedgerTransactions? @relation("transaction_approvals_transaction_id", fields: [transaction_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("transaction_approvals")
}

model UserActivityLog {
  activity_type String
  created_at String
  description String
  id String @id @default(uuid())
  metadata Json?
  performed_by String?

  @@map("user_activity_log")
}

model UserLoanRepayments {
  amount Float
  created_at String
  id String @id @default(uuid())
  payment_method String

  loan_id String?
  loan_rel UserLoans? @relation("user_loan_repayments_loan_id", fields: [loan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("user_loan_repayments")
}

model UserLoans {
  agent_verified Boolean?
  agent_verified_at String?
  agent_verified_by String?
  ai_insurance_accepted Boolean?
  ai_insurance_accepted_at String?
  amount Float
  created_at String
  due_date String
  id String @id @default(uuid())
  interest_rate Float
  paid_amount Float
  repaid_at String?
  repayment_frequency String?
  status String
  total_repayment Float

  credit_request_details_loan_id CreditRequestDetails[] @relation("credit_request_details_loan_id")
  user_loan_repayments_loan_id UserLoanRepayments[] @relation("user_loan_repayments_loan_id")
  @@map("user_loans")
}

model UserLocations {
  accuracy Float?
  address String?
  captured_at String
  city String?
  country String?
  created_at String
  id String @id @default(uuid())
  latitude Float
  longitude Float
  verification_notes String?
  verified Boolean?
  verified_at String?
  verified_by String?

  @@map("user_locations")
}

model UserReceipts {
  claimed_amount Float
  created_at String
  id String @id @default(uuid())
  items_description String
  loan_contribution Float?
  rejection_reason String?
  verified Boolean
  verified_at String?

  receipt_number_id String?
  receipt_number_rel ReceiptNumbers? @relation("user_receipts_receipt_number_id", fields: [receipt_number_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("user_receipts")
}

model UserReviews {
  created_at String
  id String @id @default(uuid())
  rating Float
  review_text String?
  reviewer_role String?
  updated_at String

  @@map("user_reviews")
}

model UserRiskScores {
  consecutive_missed_payments Float?
  consecutive_on_time_payments Float?
  created_at String?
  id String @id @default(uuid())
  last_payment_date String?
  last_risk_update String?
  notes String?
  risk_level String?
  risk_score Float?
  total_missed_payments Float?
  total_on_time_payments Float?
  updated_at String?

  @@map("user_risk_scores")
}

model UserRoles {
  created_at String
  enabled Boolean
  id String @id @default(uuid())
  role String

  @@map("user_roles")
}

model Vendors {
  id String @id @default(uuid())
  active Boolean
  created_at String
  created_by String
  location String?
  name String
  phone String?
  pin String?
  pin_hash String?

  receipt_numbers_vendor_id ReceiptNumbers[] @relation("receipt_numbers_vendor_id")
  @@map("vendors")
}

model VoidedLedgerEntries {
  id String @id @default(uuid())
  account String?
  amount Float
  category String
  created_at String
  description String?
  direction String
  linked_party String?
  reference_id String?
  running_balance Float?
  source_id String?
  source_table String
  transaction_date String
  transaction_group_id String?
  user_id String?
  void_reason String
  voided_at String
  voided_by String

  @@map("voided_ledger_entries")
}

model WalletDeposits {
  amount Float
  created_at String
  deposit_type String
  id String @id @default(uuid())

  @@map("wallet_deposits")
}

model WalletTransactions {
  amount Float
  created_at String
  description String?
  id String @id @default(uuid())

  @@map("wallet_transactions")
}

model Wallets {
  balance Float
  created_at String
  id String @id @default(uuid())
  updated_at String

  @@map("wallets")
}

model WelileHomesSubscriptions {
  created_at String
  email_statements_enabled Boolean?
  id String @id @default(uuid())
  landlord_id String?
  landlord_registered Boolean
  last_interest_applied_at String?
  last_statement_sent_at String?
  monthly_rent Float
  months_enrolled Float
  notes String?
  subscription_status String
  total_savings Float
  updated_at String

  @@map("welile_homes_subscriptions")
}

model Wishlists {
  created_at String
  id String @id @default(uuid())

  product_id String?
  product_rel Products? @relation("wishlists_product_id", fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@map("wishlists")
}

model WithdrawalRequests {
  amount Float
  cfo_approved_at String?
  cfo_approved_by String?
  coo_approved_at String?
  coo_approved_by String?
  created_at String
  id String @id @default(uuid())
  manager_approved_at String?
  manager_approved_by String?
  mobile_money_name String?
  mobile_money_number String?
  mobile_money_provider String?
  processed_at String?
  processed_by String?
  rejection_reason String?
  status String
  transaction_id String?
  updated_at String

  @@map("withdrawal_requests")
}

